package ;

import yawf.JsonRpcApp;
import yawf.redis.RedisClient;

class Main {
    public static function main() {
    	/*
    	var z:JsonRpcApp = new JsonRpcApp();
    	z.init(function () {
    		z.start();
    	});*/
    }
}
